{
  "name": "quiz-web-app",
  "version": "1.0.0",
  "description": "Quiz web application with JSON database and Monica AI integration",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "echo 'No build needed'",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.23"
  }
}
