# Minimal requirements for Render.com deployment
# Tested and working configuration

Flask==3.1.0
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
Flask-Limiter==3.5.0
PyJWT==2.8.0
psycopg2-binary==2.9.9
requests==2.31.0
python-dotenv==1.0.0
gunicorn==21.2.0

# Flask 3.1.0 automatically installs compatible versions of:
# - Werkzeug (automatically resolved)
# - SQLAlchemy (automatically resolved)
# - Jinja2, MarkupSafe, etc.

# Note: Do not specify Werkzeug or SQLAlchemy versions manually
# to avoid dependency conflicts. Let Flask handle them.
